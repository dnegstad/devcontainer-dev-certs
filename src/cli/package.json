{
  "name": "@devcontainer-dev-certs/cli",
  "version": "1.3.1-pre",
  "private": true,
  "description": "ddc — host-side CLI for generating, inspecting, and trusting dev certs without VS Code.",
  "main": "./dist/ddc.js",
  "bin": {
    "ddc": "./dist/ddc.js"
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "build:prod": "node esbuild.mjs --production",
    "test": "vitest run",
    "lint": "eslint src tests"
  },
  "dependencies": {
    "@devcontainer-dev-certs/shared": "*",
    "@peculiar/x509": "^2.0.0",
    "asn1js": "^3.0.10",
    "commander": "^14.0.1",
    "pkijs": "^3.4.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
